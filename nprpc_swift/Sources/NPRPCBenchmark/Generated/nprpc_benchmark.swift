// Generated by npidl compiler
// DO NOT EDIT - all changes will be lost

import NPRPC

public protocol ServerControlProtocol {
  func shutdown()
}

// Client proxy for ServerControl
// Pure Swift implementation with direct marshalling
final public class ServerControl: NPRPCObject, @unchecked Sendable {
  public override class var classId: String {
    "nprpc_benchmark/nprpc.benchmark.ServerControl"
  }

  public required init(handle: UnsafeMutableRawPointer)   {
    super.init(handle: handle)
  }

  public required init(from decoder: Decoder) throws   {
    try super.init(from: decoder)
  }

  public func shutdown() async throws   {
    // Prepare buffer
    let buffer = FlatBuffer()
    buffer.prepare(32)
    buffer.commit(32)
    guard let bufData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }

    // Write message header
    bufData.storeBytes(of: UInt32(0), toByteOffset: 0, as: UInt32.self)  // size (set later)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 4, as: UInt32.self)  // msg_id: FunctionCall (MessageId enum value 0)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 8, as: UInt32.self)  // msg_type: Request
    bufData.storeBytes(of: UInt32(0), toByteOffset: 12, as: UInt32.self) // reserved

    // Write call header
    bufData.storeBytes(of: poaIdx, toByteOffset: 16, as: UInt16.self)
    bufData.storeBytes(of: UInt8(0), toByteOffset: 18, as: UInt8.self)  // interface_idx
    bufData.storeBytes(of: UInt8(0), toByteOffset: 19, as: UInt8.self)  // function_idx
    bufData.storeBytes(of: objectId, toByteOffset: 24, as: UInt64.self)

    guard let finalData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }
    finalData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)

    // Send and receive
    try Task.checkCancellation()
    let responseBuffer = try await sendAsyncReceive(buffer: buffer, timeout: timeout)

    // Handle reply
    let stdReply = try handleStandardReply(buffer: responseBuffer)
    if stdReply != 0 { throw UnexpectedReplyError(message: "Unexpected reply") }
  }

}

// Servant base for ServerControl
open class ServerControlServant: NPRPCServant, ServerControlProtocol, @unchecked Sendable {
  public override init() { super.init() }

  public override func getClass() -> String   {
    return "nprpc_benchmark/nprpc.benchmark.ServerControl"
  }

  open func shutdown()   {
    fatalError("Subclass must implement shutdown")
  }

  // Dispatch incoming RPC calls
  public override func dispatch(buffer: FlatBuffer, remoteEndpoint: NPRPCEndpoint)   {
    guard let data = buffer.data else { return }

    // Read function index from CallHeader
    let functionIdx = data.load(fromByteOffset: (16 + MemoryLayout<NPRPC.impl.CallHeader>.offset(of: \NPRPC.impl.CallHeader.function_idx)!), as: UInt8.self)

    switch functionIdx     {
      case 0: // Shutdown
        
        shutdown()
        // Send success
        makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Success)
      default:
        makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Error_UnknownFunctionIdx)
    } // switch
  } // dispatch
}

public struct Person: Codable, Sendable {
  public var name: String = ""
  public var age: UInt32 = 0
  public var email: String = ""

  public init() {}

  public init(name: String, age: UInt32, email: String)   {
    self.name = name
    self.age = age
    self.email = email
  }
}


// MARK: - Marshal Person
public func marshal_Person(buffer: FlatBuffer, offset: Int, data: Person) {
  NPRPC.marshal_string(buffer: buffer, offset: offset + 0, string: data.name)
  buffer.storeBytes(of: data.age, toByteOffset: offset + 8, as: UInt32.self)
  NPRPC.marshal_string(buffer: buffer, offset: offset + 12, string: data.email)
}

// MARK: - Unmarshal Person
public func unmarshal_Person(buffer: UnsafeRawPointer, offset: Int) -> Person {
  var result = Person()
  result.name = NPRPC.unmarshal_string(buffer: buffer, offset: offset + 0)
  result.age = buffer.load(fromByteOffset: offset + 8, as: UInt32.self)
  result.email = NPRPC.unmarshal_string(buffer: buffer, offset: offset + 12)
  return result
}

public struct Address: Codable, Sendable {
  public var street: String = ""
  public var city: String = ""
  public var country: String = ""
  public var zipCode: UInt32 = 0

  public init() {}

  public init(street: String, city: String, country: String, zipCode: UInt32)   {
    self.street = street
    self.city = city
    self.country = country
    self.zipCode = zipCode
  }
}


// MARK: - Marshal Address
public func marshal_Address(buffer: FlatBuffer, offset: Int, data: Address) {
  NPRPC.marshal_string(buffer: buffer, offset: offset + 0, string: data.street)
  NPRPC.marshal_string(buffer: buffer, offset: offset + 8, string: data.city)
  NPRPC.marshal_string(buffer: buffer, offset: offset + 16, string: data.country)
  buffer.storeBytes(of: data.zipCode, toByteOffset: offset + 24, as: UInt32.self)
}

// MARK: - Unmarshal Address
public func unmarshal_Address(buffer: UnsafeRawPointer, offset: Int) -> Address {
  var result = Address()
  result.street = NPRPC.unmarshal_string(buffer: buffer, offset: offset + 0)
  result.city = NPRPC.unmarshal_string(buffer: buffer, offset: offset + 8)
  result.country = NPRPC.unmarshal_string(buffer: buffer, offset: offset + 16)
  result.zipCode = buffer.load(fromByteOffset: offset + 24, as: UInt32.self)
  return result
}

public struct Employee: Codable, Sendable {
  public var person: Person = Person()
  public var address: Address = Address()
  public var employeeId: UInt64 = 0
  public var salary: Double = 0.0
  public var skills: [String] = []

  public init() {}

  public init(person: Person, address: Address, employeeId: UInt64, salary: Double, skills: [String])   {
    self.person = person
    self.address = address
    self.employeeId = employeeId
    self.salary = salary
    self.skills = skills
  }
}


// MARK: - Marshal Employee
public func marshal_Employee(buffer: FlatBuffer, offset: Int, data: Employee) {
  marshal_Person(buffer: buffer, offset: offset + 0, data: data.person)
  marshal_Address(buffer: buffer, offset: offset + 20, data: data.address)
  buffer.storeBytes(of: data.employeeId, toByteOffset: offset + 48, as: UInt64.self)
  buffer.storeBytes(of: data.salary, toByteOffset: offset + 56, as: Double.self)
  NPRPC.marshal_string_vector(buffer: buffer, offset: offset + 64, vector: data.skills)
}

// MARK: - Unmarshal Employee
public func unmarshal_Employee(buffer: UnsafeRawPointer, offset: Int) -> Employee {
  var result = Employee()
  result.person = unmarshal_Person(buffer: buffer, offset: offset + 0)
  result.address = unmarshal_Address(buffer: buffer, offset: offset + 20)
  result.employeeId = buffer.load(fromByteOffset: offset + 48, as: UInt64.self)
  result.salary = buffer.load(fromByteOffset: offset + 56, as: Double.self)
  result.skills = NPRPC.unmarshal_string_vector(buffer: buffer, offset: offset + 64)
  return result
}

fileprivate struct nprpc_benchmark_M1: Codable, Sendable {
  public var _1: UInt32 = 0
  public var _2: UInt32 = 0

  public init() {}

  public init(_1: UInt32, _2: UInt32)   {
    self._1 = _1
    self._2 = _2
  }
}


// MARK: - Marshal nprpc_benchmark_M1
fileprivate func marshal_nprpc_benchmark_M1(buffer: FlatBuffer, offset: Int, data: nprpc_benchmark_M1) {
  buffer.storeBytes(of: data._1, toByteOffset: offset + 0, as: UInt32.self)
  buffer.storeBytes(of: data._2, toByteOffset: offset + 4, as: UInt32.self)
}

// MARK: - Unmarshal nprpc_benchmark_M1
fileprivate func unmarshal_nprpc_benchmark_M1(buffer: UnsafeRawPointer, offset: Int) -> nprpc_benchmark_M1 {
  var result = nprpc_benchmark_M1()
  result._1 = buffer.load(fromByteOffset: offset + 0, as: UInt32.self)
  result._2 = buffer.load(fromByteOffset: offset + 4, as: UInt32.self)
  return result
}

fileprivate struct nprpc_benchmark_M2: Codable, Sendable {
  public var _1: UInt32 = 0

  public init() {}

  public init(_1: UInt32)   {
    self._1 = _1
  }
}


// MARK: - Marshal nprpc_benchmark_M2
fileprivate func marshal_nprpc_benchmark_M2(buffer: FlatBuffer, offset: Int, data: nprpc_benchmark_M2) {
  buffer.storeBytes(of: data._1, toByteOffset: offset + 0, as: UInt32.self)
}

// MARK: - Unmarshal nprpc_benchmark_M2
fileprivate func unmarshal_nprpc_benchmark_M2(buffer: UnsafeRawPointer, offset: Int) -> nprpc_benchmark_M2 {
  var result = nprpc_benchmark_M2()
  result._1 = buffer.load(fromByteOffset: offset + 0, as: UInt32.self)
  return result
}

fileprivate struct nprpc_benchmark_M3: Codable, Sendable {
  public var _1: String = ""

  public init() {}

  public init(_1: String)   {
    self._1 = _1
  }
}


// MARK: - Marshal nprpc_benchmark_M3
fileprivate func marshal_nprpc_benchmark_M3(buffer: FlatBuffer, offset: Int, data: nprpc_benchmark_M3) {
  NPRPC.marshal_string(buffer: buffer, offset: offset + 0, string: data._1)
}

// MARK: - Unmarshal nprpc_benchmark_M3
fileprivate func unmarshal_nprpc_benchmark_M3(buffer: UnsafeRawPointer, offset: Int) -> nprpc_benchmark_M3 {
  var result = nprpc_benchmark_M3()
  result._1 = NPRPC.unmarshal_string(buffer: buffer, offset: offset + 0)
  return result
}

fileprivate struct nprpc_benchmark_M4: Codable, Sendable {
  public var _1: Employee = Employee()

  public init() {}

  public init(_1: Employee)   {
    self._1 = _1
  }
}


// MARK: - Marshal nprpc_benchmark_M4
fileprivate func marshal_nprpc_benchmark_M4(buffer: FlatBuffer, offset: Int, data: nprpc_benchmark_M4) {
  marshal_Employee(buffer: buffer, offset: offset + 0, data: data._1)
}

// MARK: - Unmarshal nprpc_benchmark_M4
fileprivate func unmarshal_nprpc_benchmark_M4(buffer: UnsafeRawPointer, offset: Int) -> nprpc_benchmark_M4 {
  var result = nprpc_benchmark_M4()
  result._1 = unmarshal_Employee(buffer: buffer, offset: offset + 0)
  return result
}

fileprivate struct nprpc_benchmark_M5: Codable, Sendable {
  public var _1: [UInt8] = []

  public init() {}

  public init(_1: [UInt8])   {
    self._1 = _1
  }
}


// MARK: - Marshal nprpc_benchmark_M5
fileprivate func marshal_nprpc_benchmark_M5(buffer: FlatBuffer, offset: Int, data: nprpc_benchmark_M5) {
  NPRPC.marshal_fundamental_vector(buffer: buffer, offset: offset + 0, vector: data._1)
}

// MARK: - Unmarshal nprpc_benchmark_M5
fileprivate func unmarshal_nprpc_benchmark_M5(buffer: UnsafeRawPointer, offset: Int) -> nprpc_benchmark_M5 {
  var result = nprpc_benchmark_M5()
  result._1 = NPRPC.unmarshal_fundamental_vector(buffer: buffer, offset: offset + 0)
  return result
}

public protocol BenchmarkProtocol {
  func ping()
  func func1(a: UInt32, b: UInt32) -> UInt32
  func func2(data: String)
  func processEmployee(employee: Employee) -> Employee
  func processLargeData(data: [UInt8]) -> [UInt8]
}

// Client proxy for Benchmark
// Pure Swift implementation with direct marshalling
final public class Benchmark: NPRPCObject, @unchecked Sendable {
  public override class var classId: String {
    "nprpc_benchmark/nprpc.benchmark.Benchmark"
  }

  public required init(handle: UnsafeMutableRawPointer)   {
    super.init(handle: handle)
  }

  public required init(from decoder: Decoder) throws   {
    try super.init(from: decoder)
  }

  public func ping() async throws   {
    // Prepare buffer
    let buffer = FlatBuffer()
    buffer.prepare(32)
    buffer.commit(32)
    guard let bufData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }

    // Write message header
    bufData.storeBytes(of: UInt32(0), toByteOffset: 0, as: UInt32.self)  // size (set later)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 4, as: UInt32.self)  // msg_id: FunctionCall (MessageId enum value 0)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 8, as: UInt32.self)  // msg_type: Request
    bufData.storeBytes(of: UInt32(0), toByteOffset: 12, as: UInt32.self) // reserved

    // Write call header
    bufData.storeBytes(of: poaIdx, toByteOffset: 16, as: UInt16.self)
    bufData.storeBytes(of: UInt8(0), toByteOffset: 18, as: UInt8.self)  // interface_idx
    bufData.storeBytes(of: UInt8(0), toByteOffset: 19, as: UInt8.self)  // function_idx
    bufData.storeBytes(of: objectId, toByteOffset: 24, as: UInt64.self)

    guard let finalData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }
    finalData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)

    // Send and receive
    try Task.checkCancellation()
    let responseBuffer = try await sendAsyncReceive(buffer: buffer, timeout: timeout)

    // Handle reply
    let stdReply = try handleStandardReply(buffer: responseBuffer)
    if stdReply != 0 { throw UnexpectedReplyError(message: "Unexpected reply") }
  }

  public func func1(a: UInt32, b: UInt32) async throws -> UInt32   {
    // Prepare buffer
    let buffer = FlatBuffer()
    buffer.prepare(40)
    buffer.commit(40)
    guard let bufData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }

    // Write message header
    bufData.storeBytes(of: UInt32(0), toByteOffset: 0, as: UInt32.self)  // size (set later)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 4, as: UInt32.self)  // msg_id: FunctionCall (MessageId enum value 0)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 8, as: UInt32.self)  // msg_type: Request
    bufData.storeBytes(of: UInt32(0), toByteOffset: 12, as: UInt32.self) // reserved

    // Write call header
    bufData.storeBytes(of: poaIdx, toByteOffset: 16, as: UInt16.self)
    bufData.storeBytes(of: UInt8(0), toByteOffset: 18, as: UInt8.self)  // interface_idx
    bufData.storeBytes(of: UInt8(1), toByteOffset: 19, as: UInt8.self)  // function_idx
    bufData.storeBytes(of: objectId, toByteOffset: 24, as: UInt64.self)

    // Marshal input arguments
    var inArgs = nprpc_benchmark_M1()
    inArgs._1 = a
    inArgs._2 = b
    marshal_nprpc_benchmark_M1(buffer: buffer, offset: 32, data: inArgs)

    guard let finalData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }
    finalData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)

    // Send and receive
    try Task.checkCancellation()
    let responseBuffer = try await sendAsyncReceive(buffer: buffer, timeout: timeout)

    // Handle reply
    let stdReply = try handleStandardReply(buffer: responseBuffer)
    if stdReply != -1 { throw UnexpectedReplyError(message: "Unexpected reply") }

    guard let responseData = responseBuffer.data else { throw BufferError(message: "Failed to get response data") }
    let out = unmarshal_nprpc_benchmark_M2(buffer: responseData, offset: 16)
    return out._1
  }

  public func func2(data: String) async throws   {
    // Prepare buffer
    let buffer = FlatBuffer()
    buffer.prepare(168)
    buffer.commit(40)
    guard let bufData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }

    // Write message header
    bufData.storeBytes(of: UInt32(0), toByteOffset: 0, as: UInt32.self)  // size (set later)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 4, as: UInt32.self)  // msg_id: FunctionCall (MessageId enum value 0)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 8, as: UInt32.self)  // msg_type: Request
    bufData.storeBytes(of: UInt32(0), toByteOffset: 12, as: UInt32.self) // reserved

    // Write call header
    bufData.storeBytes(of: poaIdx, toByteOffset: 16, as: UInt16.self)
    bufData.storeBytes(of: UInt8(0), toByteOffset: 18, as: UInt8.self)  // interface_idx
    bufData.storeBytes(of: UInt8(2), toByteOffset: 19, as: UInt8.self)  // function_idx
    bufData.storeBytes(of: objectId, toByteOffset: 24, as: UInt64.self)

    // Marshal input arguments
    var inArgs = nprpc_benchmark_M3()
    inArgs._1 = data
    marshal_nprpc_benchmark_M3(buffer: buffer, offset: 32, data: inArgs)

    guard let finalData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }
    finalData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)

    // Send and receive
    try Task.checkCancellation()
    let responseBuffer = try await sendAsyncReceive(buffer: buffer, timeout: timeout)

    // Handle reply
    let stdReply = try handleStandardReply(buffer: responseBuffer)
    if stdReply != 0 { throw UnexpectedReplyError(message: "Unexpected reply") }
  }

  public func processEmployee(employee: Employee) async throws -> Employee   {
    // Prepare buffer
    let buffer = FlatBuffer()
    buffer.prepare(232)
    buffer.commit(104)
    guard let bufData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }

    // Write message header
    bufData.storeBytes(of: UInt32(0), toByteOffset: 0, as: UInt32.self)  // size (set later)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 4, as: UInt32.self)  // msg_id: FunctionCall (MessageId enum value 0)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 8, as: UInt32.self)  // msg_type: Request
    bufData.storeBytes(of: UInt32(0), toByteOffset: 12, as: UInt32.self) // reserved

    // Write call header
    bufData.storeBytes(of: poaIdx, toByteOffset: 16, as: UInt16.self)
    bufData.storeBytes(of: UInt8(0), toByteOffset: 18, as: UInt8.self)  // interface_idx
    bufData.storeBytes(of: UInt8(3), toByteOffset: 19, as: UInt8.self)  // function_idx
    bufData.storeBytes(of: objectId, toByteOffset: 24, as: UInt64.self)

    // Marshal input arguments
    var inArgs = nprpc_benchmark_M4()
    inArgs._1 = employee
    marshal_nprpc_benchmark_M4(buffer: buffer, offset: 32, data: inArgs)

    guard let finalData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }
    finalData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)

    // Send and receive
    try Task.checkCancellation()
    let responseBuffer = try await sendAsyncReceive(buffer: buffer, timeout: timeout)

    // Handle reply
    let stdReply = try handleStandardReply(buffer: responseBuffer)
    if stdReply != -1 { throw UnexpectedReplyError(message: "Unexpected reply") }

    guard let responseData = responseBuffer.data else { throw BufferError(message: "Failed to get response data") }
    let out = unmarshal_nprpc_benchmark_M4(buffer: responseData, offset: 16)
    return out._1
  }

  public func processLargeData(data: [UInt8]) async throws -> [UInt8]   {
    // Prepare buffer
    let buffer = FlatBuffer()
    buffer.prepare(168)
    buffer.commit(40)
    guard let bufData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }

    // Write message header
    bufData.storeBytes(of: UInt32(0), toByteOffset: 0, as: UInt32.self)  // size (set later)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 4, as: UInt32.self)  // msg_id: FunctionCall (MessageId enum value 0)
    bufData.storeBytes(of: UInt32(0), toByteOffset: 8, as: UInt32.self)  // msg_type: Request
    bufData.storeBytes(of: UInt32(0), toByteOffset: 12, as: UInt32.self) // reserved

    // Write call header
    bufData.storeBytes(of: poaIdx, toByteOffset: 16, as: UInt16.self)
    bufData.storeBytes(of: UInt8(0), toByteOffset: 18, as: UInt8.self)  // interface_idx
    bufData.storeBytes(of: UInt8(4), toByteOffset: 19, as: UInt8.self)  // function_idx
    bufData.storeBytes(of: objectId, toByteOffset: 24, as: UInt64.self)

    // Marshal input arguments
    var inArgs = nprpc_benchmark_M5()
    inArgs._1 = data
    marshal_nprpc_benchmark_M5(buffer: buffer, offset: 32, data: inArgs)

    guard let finalData = buffer.data else { throw BufferError(message: "Failed to get buffer data") }
    finalData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)

    // Send and receive
    try Task.checkCancellation()
    let responseBuffer = try await sendAsyncReceive(buffer: buffer, timeout: timeout)

    // Handle reply
    let stdReply = try handleStandardReply(buffer: responseBuffer)
    if stdReply != -1 { throw UnexpectedReplyError(message: "Unexpected reply") }

    guard let responseData = responseBuffer.data else { throw BufferError(message: "Failed to get response data") }
    let out = unmarshal_nprpc_benchmark_M5(buffer: responseData, offset: 16)
    return out._1
  }

}

// Servant base for Benchmark
open class BenchmarkServant: NPRPCServant, BenchmarkProtocol, @unchecked Sendable {
  public override init() { super.init() }

  public override func getClass() -> String   {
    return "nprpc_benchmark/nprpc.benchmark.Benchmark"
  }

  open func ping()   {
    fatalError("Subclass must implement ping")
  }

  open func func1(a: UInt32, b: UInt32) -> UInt32   {
    fatalError("Subclass must implement func1")
  }

  open func func2(data: String)   {
    fatalError("Subclass must implement func2")
  }

  open func processEmployee(employee: Employee) -> Employee   {
    fatalError("Subclass must implement processEmployee")
  }

  open func processLargeData(data: [UInt8]) -> [UInt8]   {
    fatalError("Subclass must implement processLargeData")
  }

  // Dispatch incoming RPC calls
  public override func dispatch(buffer: FlatBuffer, remoteEndpoint: NPRPCEndpoint)   {
    guard let data = buffer.data else { return }

    // Read function index from CallHeader
    let functionIdx = data.load(fromByteOffset: (16 + MemoryLayout<NPRPC.impl.CallHeader>.offset(of: \NPRPC.impl.CallHeader.function_idx)!), as: UInt8.self)

    switch functionIdx     {
      case 0: // Ping
        
        ping()
        // Send success
        makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Success)
      case 1: // Func1
        // Validate input buffer for untrusted interface
        guard check_1Fu322Fu32(buffer: data, bufferSize: buffer.size, offset: 32) else         {
          makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Error_BadInput)
          return
        }

        // Unmarshal input arguments
        let ia = unmarshal_nprpc_benchmark_M1(buffer: data, offset: 32)
        
        let __ret_val = func1(a: ia._1, b: ia._2)
        // Prepare output buffer
        let obuf = buffer
        obuf.consume(obuf.size)
        obuf.prepare(20)
        obuf.commit(20)
        // Marshal output arguments
        var out_data = nprpc_benchmark_M2()
        out_data._1 = __ret_val

        marshal_nprpc_benchmark_M2(buffer: buffer, offset: 16, data: out_data)
        guard let outData = buffer.data else { return }
        outData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)
        outData.storeBytes(of: impl.MessageId.BlockResponse.rawValue, toByteOffset: 4, as: UInt32.self)
        outData.storeBytes(of: impl.MessageType.Answer.rawValue, toByteOffset: 8, as: UInt32.self)
      case 2: // Func2
        // Validate input buffer for untrusted interface
        guard check_1S(buffer: data, bufferSize: buffer.size, offset: 32) else         {
          makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Error_BadInput)
          return
        }

        // Unmarshal input arguments
        let ia = unmarshal_nprpc_benchmark_M3(buffer: data, offset: 32)
        
        func2(data: ia._1)
        // Send success
        makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Success)
      case 3: // ProcessEmployee
        // Validate input buffer for untrusted interface
        guard check_1Employee_1(buffer: data, bufferSize: buffer.size, offset: 32) else         {
          makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Error_BadInput)
          return
        }

        // Unmarshal input arguments
        let ia = unmarshal_nprpc_benchmark_M4(buffer: data, offset: 32)
        // Prepare output buffer
        let obuf = buffer
        obuf.consume(obuf.size)
        obuf.prepare(216)
        obuf.commit(88)
        
        let __ret_val = processEmployee(employee: ia._1)
        // Marshal output arguments
        var out_data = nprpc_benchmark_M4()
        out_data._1 = __ret_val

        marshal_nprpc_benchmark_M4(buffer: buffer, offset: 16, data: out_data)
        guard let outData = buffer.data else { return }
        outData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)
        outData.storeBytes(of: impl.MessageId.BlockResponse.rawValue, toByteOffset: 4, as: UInt32.self)
        outData.storeBytes(of: impl.MessageType.Answer.rawValue, toByteOffset: 8, as: UInt32.self)
      case 4: // ProcessLargeData
        // Validate input buffer for untrusted interface
        guard check_1VFu8(buffer: data, bufferSize: buffer.size, offset: 32) else         {
          makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Error_BadInput)
          return
        }

        // Unmarshal input arguments
        let ia = unmarshal_nprpc_benchmark_M5(buffer: data, offset: 32)
        // Prepare output buffer
        let obuf = buffer
        obuf.consume(obuf.size)
        obuf.prepare(152)
        obuf.commit(24)
        
        let _out_result = processLargeData(data: ia._1)
        // Marshal output arguments
        var out_data = nprpc_benchmark_M5()
        out_data._1 = _out_result

        marshal_nprpc_benchmark_M5(buffer: buffer, offset: 16, data: out_data)
        guard let outData = buffer.data else { return }
        outData.storeBytes(of: UInt32(buffer.size), toByteOffset: 0, as: UInt32.self)
        outData.storeBytes(of: impl.MessageId.BlockResponse.rawValue, toByteOffset: 4, as: UInt32.self)
        outData.storeBytes(of: impl.MessageType.Answer.rawValue, toByteOffset: 8, as: UInt32.self)
      default:
        makeSimpleAnswer(buffer: buffer, messageId: impl.MessageId.Error_UnknownFunctionIdx)
    } // switch
  } // dispatch
}


// Safety check for nprpc_benchmark_M1
fileprivate func check_1Fu322Fu32(buffer: UnsafeRawPointer, bufferSize: Int, offset: Int) -> Bool {
  guard NPRPC.check_struct_bounds(bufferSize: bufferSize, offset: offset, structSize: 8) else { return false }
  return true
}


// Safety check for nprpc_benchmark_M3
fileprivate func check_1S(buffer: UnsafeRawPointer, bufferSize: Int, offset: Int) -> Bool {
  guard NPRPC.check_struct_bounds(bufferSize: bufferSize, offset: offset, structSize: 8) else { return false }
  guard NPRPC.check_string_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0) else { return false }
  return true
}


// Safety check for nprpc_benchmark_M4
fileprivate func check_1Employee_1(buffer: UnsafeRawPointer, bufferSize: Int, offset: Int) -> Bool {
  guard NPRPC.check_struct_bounds(bufferSize: bufferSize, offset: offset, structSize: 72) else { return false }
  guard NPRPC.check_string_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0 + 0 + 0) else { return false }
  guard NPRPC.check_string_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0 + 0 + 12) else { return false }
  guard NPRPC.check_string_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0 + 20 + 0) else { return false }
  guard NPRPC.check_string_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0 + 20 + 8) else { return false }
  guard NPRPC.check_string_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0 + 20 + 16) else { return false }
  guard NPRPC.check_vector_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0 + 64, elementSize: 8) else { return false }
  do {
    let relOffset = Int(buffer.load(fromByteOffset: offset + 0 + 64, as: UInt32.self))
    let count = Int(buffer.load(fromByteOffset: offset + 0 + 64 + 4, as: UInt32.self))
    let dataOffset = offset + 0 + 64 + relOffset
    for i in 0..<count {
      let elemOffset = dataOffset + i * 8
      guard NPRPC.check_string_bounds(buffer: buffer, bufferSize: bufferSize, offset: elemOffset) else { return false }
    }
  }
  return true
}


// Safety check for nprpc_benchmark_M5
fileprivate func check_1VFu8(buffer: UnsafeRawPointer, bufferSize: Int, offset: Int) -> Bool {
  guard NPRPC.check_struct_bounds(bufferSize: bufferSize, offset: offset, structSize: 8) else { return false }
  guard NPRPC.check_vector_bounds(buffer: buffer, bufferSize: bufferSize, offset: offset + 0, elementSize: 1) else { return false }
  return true
}

