// Simple test IDL for Swift bindings
module swift.test;

// exception TestException {
//     message: string;
//     code: u32;
// }

enum Color {
  Red,
  Green,
  Blue
}

message Point {
  x: i32;
  y: i32;
}

message Rectangle {
  topLeft: Point;
  bottomRight: Point;
  color: Color;
}

interface ShapeService {
  void getRectangle(id: u32, rect: out Rectangle);
  void setRectangle(id: u32, rect: Rectangle);
}

